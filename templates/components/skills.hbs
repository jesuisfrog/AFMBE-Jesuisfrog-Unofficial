<div class="skills-container">
    <h2 id="skill-header" class="header-with-button">
        <button type="button" class="create-item" data-create="skill">+</button>
        <span>{{localize 'AFMBE.Skills.Title'}}</span>
    </h2>
    <table class="item-table">
        <thead>
            <tr>
                <th class="table-left-align">{{localize 'AFMBE.Common.Name'}}</th>
                <th>{{localize 'AFMBE.Common.Level'}}</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {{#each actor.skill as |item id|}}
            <tr class="item" data-item-id="{{item._id}}">
                <td class="item-name">{{item.name}}</td>
                <td class="table-center-align">{{item.system.level}}</td>
                <td class="table-center-align"><a class="item-control item-delete" title="{{localize 'AFMBE.Items.Delete'}}"><i
                            class="fas fa-trash"></i></a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>
