<div class="item-description-sidebar">
    <h2 class="item-desc">{{localize 'AFMBE.Common.Description'}}</h2>
    {{#if editable}}
    <prose-mirror name="system.description" button="true" editable="{{editable}}" toggled="false"
        value="{{item.system.description}}">
        {{{descriptionHTML}}}
    </prose-mirror>
    {{else}}
    {{{descriptionHTML}}}
    {{/if}}
</div>
